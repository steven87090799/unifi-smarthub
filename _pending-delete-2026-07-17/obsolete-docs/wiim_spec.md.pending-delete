待確認刪除：本文件的 10 秒記憶體歷史設計已被 SQLite 與 3 秒活動取樣取代。

# WiiM Amp 戰情室整合 API 技術規格書 (wiim_spec.md)

本規格書詳細記錄 `unifi-smarthub` 專案中整合 WiiM Amp 串流音響的 API 設計、傳輸頻率、底層指令映射以及資料欄位定義。

---

## 一、 系統架構與安全機制
* **安全性繞過**：新版 WiiM 韌體對直接 IP 存取進行了安全限制（會返回 `Direct IP access is not allowed`）。後端代理程式在請求時，會自動在標頭中寫入 `User-Agent: wiim-temp/2.0` 以安全繞過此防禦。
* **SSL 證書忽略**：WiiM 使用自簽憑證，後端 Node.js 使用 `https.Agent({ rejectUnauthorized: false })` 自動忽略憑證校驗，保證區域網路通訊穩定。

---

## 二、 戰情室 API 端點定義與傳輸頻率

### 1. 🎵 播放器狀態與曲目資訊 API
* **端點**：`GET /api/wiim/status?type=play`
* **發送頻率**：
  * **前端瀏覽器**：當網頁處於「WiiM 音響」分頁時，每 **10 秒** 自動輪詢一次。
  * **本地進度模擬**：前端使用 `setInterval` 每 **1 秒** 依據當前 `curpos` 進行預測累加，達到流暢的毫秒級進度條更新，避免高頻率呼叫。
* **底層映射命令**：
  * `getPlayerStatus`：獲取設備播放狀態。
  * `getMetaInfo`：獲取當前曲目的中繼資料。
* **可取得的詳細資訊**：
  * `player.status`：播放狀態（`play`, `pause`, `stop`, `loading`）。
  * `player.vol`：當前音量值（`0` - `100`）。
  * `player.mute`：靜音狀態（`1` 靜音，`0` 解除）。
  * `player.mode`：播放模式（`10` 網路串流, `41` 藍牙, `40` Line-in 等）。
  * `player.curpos`：曲目目前播放進度（毫秒, `ms`）。
  * `player.totlen`：曲目總長度（毫秒, `ms`）。
  * `meta.metaData.title`：當前播放歌曲標題。
  * `meta.metaData.artist`：歌手名稱。
  * `meta.metaData.album`：專輯名稱.
  * `meta.metaData.albumArtURI`：專輯封面圖片網址。
  * `meta.metaData.sampleRate`：當前音訊取樣率（如 `44100` 即 44.1 kHz）。
  * `meta.metaData.bitDepth`：音訊位元深度（如 `16` bit 或 `24` bit）。

---

### 🌡️ 2. 系統狀態與溫度監控 API
* **端點**：`GET /api/wiim/status?type=status`
* **發送頻率**：
  * **前端瀏覽器**：當網頁處於「WiiM 音響」分頁時，已依需求設定為每 **10 秒** 自動輪詢一次。
* **底層映射命令**：
  * `getStatusEx`：獲取系統底層硬體與遙測數據。
* **可取得的詳細資訊**（獲取溫度的同時，還會拿到以下所有資訊）：
  * **溫度遙測**：
    * `status.temperature_cpu`：CPU 核心溫度（攝氏度，`°C`）。
    * `status.temperature_tmp102`：主機板熱敏溫感計溫度（攝氏度，`°C`）。
  * **語音遙控器 (Voice Remote) 及藍牙配件狀態**（若配對連線）：
    * `status.bt_remote_bat` (或類似名稱)：藍牙語音遙控器剩餘電量百分比（`0` - `100`）。
    * `status.bt_remote_rssi`：遙控器藍牙訊號強度強度（`dBm`，如 `-65`）。
    * `status.bt_remote_mac`：配對的遙控器 MAC 位址。
    * `status.bt_remote_status`：遙控器連線狀態（如 `connected`）。
  * **系統與網路硬體資訊**：
    * `status.DeviceName`：音響設備在區域網路中的友善名稱。
    * `status.hardware`：晶片組與硬體主板型號。
    * `status.PCB_version`：電路板修訂版本。
    * `status.firmware`：當前系統韌體版本號。
    * `status.ssid`：所連線的 Wi-Fi 熱點名稱。
    * `status.apcli0`：設備當前獲得的實體 IP 位址。
    * `status.MAC`：設備 Wi-Fi 的 MAC 位址。
    * `status.BT_MAC`：設備藍牙模組的 MAC 位址。
    * `status.uuid`：全球唯一識別碼。

---

### 📈 3. 溫度歷史紀錄 API
* **端點**：`GET /api/wiim/history`
* **發送頻率**：
  * **前端瀏覽器**：配合系統監控，每 **10 秒** 輪詢一次，用於繪製折線圖與歷史表格。
  * **後端伺服器 (背景排程)**：每 **10 秒** 由 Node.js 背景排程向音響讀取 `temperature_cpu` 與 `temperature_tmp102`，將結果 append 至內存中的歷史陣列（最多儲存 5000 筆，不影響硬碟讀寫壽命）。
* **回傳資料結構**：
  ```json
  {
    "interval": 10,
    "cpu_alert": 70,
    "board_alert": 60,
    "data": [
      { "ts": 1783585580, "cpu": 48.5, "board": 40.2 },
      { "ts": 1783585590, "cpu": 48.2, "board": 40.1 }
    ]
  }
  ```

---

### ⚙️ 4. 遙控與控制指令 API
* **端點**：`GET /api/wiim/cmd?command=<URL_ENCODED_CMD>`
* **發送頻率**：僅在使用者點擊介面按鈕時**手動觸發**。
* **常見控制命令**：
  * `setPlayerCmd:onepause`：播放/暫停狀態切換。
  * `setPlayerCmd:vol:<0-100>`：寫入絕對音量。
  * `setPlayerCmd:mute:<1/0>`：開啟/關閉靜音。
  * `setPlayerCmd:switchmode:<mode>`：切換輸入源（`wifi`, `bluetooth`, `line-in`, `optical`, `co-axial`, `udisk`）。
  * `MCUKeyShortClick:<1-12>`：觸發預設捷徑。
  * `EQLoad:<Preset_Name>`：套用 EQ 預設。
  * `reboot`：重新啟動設備。
  * `timeSync:<YYYYMMDDHHMMSS>`：同步設備內部 RTC 時間。
  * `clear` (專屬端點 `/api/wiim/clear`)：清空歷史溫度記憶體緩衝區。

---

## 三、 效能優化與設備安全性評估
1. **零效能衝擊**：優化後，即使溫度更新頻率提高至 10 秒，平均對音響的請求數也僅有 **0.2 RPS (每秒 0.2 次請求)**。這對 WiiM 的系統負載可忽略不計。
2. **資料分流**：通過 `?type=play` 與 `?type=status`，系統在 90% 的時間內僅提取輕量的曲目資訊，避免反覆進行底層硬體硬體感測器讀取，極致呵護您的硬體壽命。
